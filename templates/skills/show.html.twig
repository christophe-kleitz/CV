{% extends 'layout/base.html.twig' %}

{% block body %}
    <h1>Skills</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ skill.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ skill.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('skills_index') }}">back to list</a>

    <a href="{{ path('skills_edit', {slug: skill.slug}) }}">edit</a>

    {{ include('skills/_delete_form.html.twig') }}
{% endblock %}
